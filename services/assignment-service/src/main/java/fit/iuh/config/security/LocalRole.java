package fit.iuh.config.security;

/**
 * Local copy of Role enum, scoped to assignment-service's security package.
 * Must stay in sync with core-service Role values.
 */
public enum LocalRole {
    ROLE_STUDENT,
    ROLE_TEACHER,
    ROLE_ADMIN
}
