import TeamDetailPageClient from './TeamDetailPageClient';

export const dynamic = 'force-dynamic';
export const fetchCache = 'force-no-store';

export default function TeamDetailPage() {
  return <TeamDetailPageClient />;
}
